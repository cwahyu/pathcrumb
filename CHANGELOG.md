# CHANGELOG

## v0.2.0

- add CLI improvements (`--version`, `--help`)
- add `--verbose` and `--quiet`
- add `--check` mode for CI
- add `pyproject.toml` configuration support
- add `pathcrumb init`
- improve header normalization (spacing, shebang handling)
- fix path resolution relative to project root
- add pre-commit hooks
- improve test coverage

## v0.1.0

- initial release
